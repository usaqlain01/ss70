<div class="col-sm-6 col-md-4">
    {% set ihref = 'images/pokemon/' ~ id ~ '.png' %}
    <div class="thumbnail poke-card" id="poke-kind{{ id }}">
        <img src="{{ asset(ihref) }}" alt="support">
        <div class="caption">
            <h3>{{ mon.name }}</h3>
            <p>...</p>
            <p><a href="#" class="btn btn-primary" role="button">Button</a> <a href="#" class="btn btn-default" role="button">MY ID IS{{ id }}</a></p>
        </div>
    </div>
</div>